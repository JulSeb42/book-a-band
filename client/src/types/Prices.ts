/*=============================================== Prices ===============================================*/

export type PricesType = {
    minPrice: number | undefined
    maxPrice: number | undefined
    globalMinPrice: number | undefined
    globalMaxPrice: number | undefined
}
