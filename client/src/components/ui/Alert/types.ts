/*=============================================== Alert types ===============================================*/

import type { ReactNode } from "react"

export interface AlertProps {
    children?: ReactNode | ReactNode[] | string
}
