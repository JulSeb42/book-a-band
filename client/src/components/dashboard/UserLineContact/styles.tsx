/*=============================================== UserLineContact styles ===============================================*/

import styled from "styled-components"

import { Text } from "components"

export const StyledUserLineContact = styled.div``

export const Name = styled(Text).attrs({ tag: "h6" })`
    line-height: 32px;
`
