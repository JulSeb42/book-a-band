/*=============================================== {{ pascalCase name }} types ===============================================*/

{{#if as}}
import type { ElementType } from "react"

{{/if}}
export interface {{ pascalCase name }}Props {
	{{#if as}}
    as?: ElementType
    {{else}}

    {{/if}}
}
