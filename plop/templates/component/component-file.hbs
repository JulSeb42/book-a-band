/*=============================================== {{ pascal name }} component ===============================================*/

{{#if forward}}
import { forwardRef, type ForwardedRef } from "react"

{{/if}}
import { Styled{{ pascal name }} } from "components/{{ pascal name }}/styles"
import type { {{ pascal name }}Props } from "components/{{ pascal name }}/types"

{{#if forward}}
export const {{ pascal name }} = forwardRef(
    (
        { {{#if as}}as{{/if}} }: {{ pascal name }}Props,
        ref?: ForwardedRef<HTML{{ capitalize tag }}Element>
    ) => {
        return (
            <Styled{{ pascal name }}{{#if as}} as={as}{{/if}} ref={ref}>
                
            </Styled{{ pascal name }}>
        )
    }
)
{{else}}
export function {{ pascal name }}({ {{#if as}}as{{/if}} }: {{ pascal name }}Props) {
	return (
		<Styled{{ pascal name }}{{#if as}} as={as}{{/if}}>

		</Styled{{ pascal name }}>
	)
}
{{/if}}
