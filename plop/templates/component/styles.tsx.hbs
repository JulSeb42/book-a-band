/*=============================================== {{ capitalize name }} styles ===============================================*/

import styled from "styled-components"

export const Styled{{ pascal name }} = styled.{{ lowerCase tag }}`
	
`
